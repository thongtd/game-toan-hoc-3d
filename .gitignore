node_modules/
dist/
# Player data and run history: local to each machine, never committed.
data/
# Per-developer permission grants; team-wide settings live in .claude/settings.json
.claude/settings.local.json
.cache/
playwright-report/
test-results/
artifacts/
.DS_Store
*.local
.env
.env.*
!.env.example

# Never commit raw asset packs or archives - only the whitelisted files
# produced by scripts/fetch-assets.* belong in public/assets/.
*.zip
